apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cortex-tenant.fullname" . }}-test-connection"
  labels:
    {{- include "cortex-tenant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cortex-tenant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
